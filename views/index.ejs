<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>URL Shortener</title>
<meta name="viewport" content="width=device-width, initial-scale=1">

<link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">

<style>
*{
  font-family: 'Poppins', sans-serif;
}
body{
  background: radial-gradient(circle at top, #0f2027, #000);
  min-height:100vh;
  color:white;
}

.container{
  max-width:900px;
  margin-top:80px;
}

.card{
  background: rgba(255,255,255,0.08);
  backdrop-filter: blur(20px);
  border-radius:15px;
  padding:30px;
  box-shadow:0 0 30px rgba(0,255,255,.2);
}

h1{
  text-align:center;
  font-weight:600;
  margin-bottom:30px;
  color:#00f7ff;
  text-shadow:0 0 15px #00f7ff;
}

input{
  background:#000;
  border:1px solid #00f7ff;
  color:white;
}

input:focus{
  box-shadow:0 0 10px #00f7ff;
  border-color:#00f7ff;
}

button{
  background:linear-gradient(45deg,#00f7ff,#00ff99);
  border:none;
  color:black;
  font-weight:bold;
}

table{
  margin-top:30px;
}

thead{
  background:#00f7ff;
  color:black;
}

tbody tr{
  transition:.3s;
}

tbody tr:hover{
  background:rgba(0,255,255,.1);
  transform:scale(1.01);
}

a{
  color:#00ffcc;
  text-decoration:none;
}

a:hover{
  color:#00f7ff;
}
</style>
</head>

<body>

<div class="container">
  <div class="card">

    <h1>🚀 URL Shortener</h1>

    <form action="/shortUrls" method="POST" class="form-inline justify-content-center mb-4">
      <input required name="fullUrl" type="url" class="form-control col-md-8 mr-2" placeholder="Paste your long URL here">
      <button class="btn">Shrink</button>
    </form>

    <table class="table table-dark table-hover">
      <thead>
        <tr>
          <th>Full URL</th>
          <th>Short</th>
          <th>Clicks</th>
        </tr>
      </thead>
      <tbody>
        <% shortUrls.forEach(url => { %>
          <tr>
            <td><a target="_blank" href="<%= url.full %>"><%= url.full %></a></td>
            <td><a target="_blank" href="/<%= url.short %>">/<%= url.short %></a></td>
            <td><%= url.clicks %></td>
          </tr>
        <% }) %>
      </tbody>
    </table>

  </div>
</div>

</body>
</html>
